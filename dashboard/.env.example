# Discord OAuth2 Configuration
DISCORD_CLIENT_ID=your_discord_client_id
# DISCORD_CLIENT_SECRET - stored as a SECRET (not env var)
DISCORD_CALLBACK_URL=https://your-repl-url.replit.dev/api/auth/callback
# DISCORD_TOKEN - stored as a SECRET (not env var)

# Database Configuration
DATABASE_URL=your_postgresql_database_url

# Session Configuration
# SESSION_SECRET - stored as a SECRET (not env var)

# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL
FRONTEND_URL=http://localhost:5000
